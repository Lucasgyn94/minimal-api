using MinimalApi;

namespace Test;

/* 
[TestClass]
public class AdministradorTest
{
    
    [TestMethod]
    public void TestarGetSetPropriedadesAdministradores()
    {
        // Arrange: onde fica todas as variáveis necessárias para as validações

        var adm = new Administrador();

        // Act: ação que vamos realizar em cima de nossos dados
        adm.Id = 1;
        adm.Email = "administrador@teste.com.br";
        adm.Senha = "123456";
        adm.Perfil = "Adm";

        // Assert: validação dos dados
        Assert.AreEqual(1, adm.Id);
        Assert.AreEqual("administrador@teste.com.br", adm.Email);
        Assert.AreEqual("123456", adm.Senha);
        Assert.AreEqual("Adm", adm.Perfil);
    }
    
}
*/